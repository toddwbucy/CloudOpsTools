# =============================================================================
# CloudOpsTools Environment Configuration
# =============================================================================
# Copy this file to .env and modify as needed.
# Configuration values are loaded at server startup.
# Restart the server after making changes.
# =============================================================================

# Application settings
APP_NAME=CloudOpsTools
DEBUG=false

# =============================================================================
# Rate Limiting Configuration
# =============================================================================
# Format: "<number>/<time_unit>"
# Time units: second, minute, hour, day
# Examples: "10/minute", "100/hour", "5/second"
#
# NOTE: Changes require server restart to take effect.
# =============================================================================

# Auth endpoints rate limit (default: 10/minute)
# Affects: POST/GET/DELETE /api/auth/aws-credentials
RATE_LIMIT_AUTH_ENDPOINTS=10/minute

# Execution endpoints rate limit (default: 5/minute)
# Affects: POST /api/tools/{tool_id}/execute
RATE_LIMIT_EXECUTION_ENDPOINTS=5/minute

# Read endpoints rate limit (default: 100/minute)
# Affects: GET /api/tools/, GET /api/tools/{tool_id}
RATE_LIMIT_READ_ENDPOINTS=100/minute

# =============================================================================
# Redis Configuration (Optional - for distributed deployments)
# =============================================================================
# When running multiple server instances, use Redis for shared rate limit state.
# If not set, uses in-memory storage (suitable for single-instance deployments).
# Format: redis://[[username]:[password]@]host[:port]/[database]
#
# REDIS_URL=redis://localhost:6379
# REDIS_URL=redis://user:password@redis.example.com:6379/0
